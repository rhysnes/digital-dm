# Campaign scaffold

Copy this directory’s contents into a new campaign root, then fill from `../templates/`.

Required first writes: `README.md`, `state/campaign.md`, `module/OVERVIEW.md`, `module/secrets.md`, `party/` sheets.
